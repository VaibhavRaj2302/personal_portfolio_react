{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start react start",
      "type": "shell",
      "command": "npm run dev",
      "group": "build",
      "isBackground": true,
      "problemMatcher": [
        "$tsc", // For TypeScript errors (if using TypeScript)
        "$eslint-stylish" // For ESLint errors (if using ESLint)
      ]
    }
  ]
}
